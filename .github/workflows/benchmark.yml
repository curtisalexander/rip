name: Windows performance experiment

on:
  push:
    branches: [perf/windows-delete]
    # Profiling and documentation pushes should not repeat the timing matrix.
    paths:
      - 'src/**'
      - 'tests/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'bench/compare.py'
      - 'bench/bench.ps1'
      - '.github/workflows/benchmark.yml'
  workflow_dispatch:
    inputs:
      baseline:
        description: Baseline commit or ref (built on the same runner)
        required: true
        default: '68ef64b3b7261d0e136a6cb37ffece44d74d63d5'

permissions:
  contents: read

jobs:
  compare:
    runs-on: windows-2022
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        shape: [flat, wide, dirs, deep, readonly]
    steps:
      - uses: actions/checkout@v6
        with:
          path: candidate
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.baseline || '68ef64b3b7261d0e136a6cb37ffece44d74d63d5' }}
          path: baseline
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Record environment (do not change Defender)
        shell: pwsh
        run: |
          Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion, OsBuildNumber, CsProcessors | ConvertTo-Json -Depth 3
          Get-Volume | Select-Object DriveLetter, FileSystem, SizeRemaining, Size | ConvertTo-Json
          $defender = Get-MpComputerStatus
          $defender | Select-Object AMProductVersion, AntivirusSignatureVersion, RealTimeProtectionEnabled | ConvertTo-Json
          if ($defender.RealTimeProtectionEnabled -ne $false) {
            throw 'This experiment requires Defender real-time protection off; settings are not changed.'
          }
          rustc -Vv
          git -C baseline rev-parse HEAD
          git -C candidate rev-parse HEAD
      - name: Check candidate
        working-directory: candidate
        run: |
          cargo fmt --check
          if ($LASTEXITCODE) { exit $LASTEXITCODE }
          cargo clippy --locked --all-targets -- -D warnings
          if ($LASTEXITCODE) { exit $LASTEXITCODE }
          cargo test --locked
          if ($LASTEXITCODE) { exit $LASTEXITCODE }
      - name: Build baseline
        working-directory: baseline
        run: cargo build --release --locked
      - name: Build candidate
        working-directory: candidate
        run: cargo build --release --locked
      - name: Paired comparison
        run: >-
          python candidate/bench/compare.py
          --baseline baseline/target/release/rip.exe
          --candidate candidate/target/release/rip.exe
          --shapes ${{ matrix.shape }}
          --entries 10000 --iterations 15 --threads 4
          --work-root candidate/target
          --settle-seconds 1
          --output comparison.json
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: comparison-${{ matrix.shape }}
          path: comparison.json
